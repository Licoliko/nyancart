# レース描画方式の検討

## 採用: 2.5D疑似3Dセグメント方式

現在のキャラクター素材は、高精細な3D風レンダーを14方向で持っています。そのため、奥行き方向に拡大・縮小する疑似3D路面と最も整合します。

- 走行距離に合わせて路面セグメント、縁石、車線、反射線を移動できる
- AI、コイン、アイテム、ゲートを同じ遠近法で配置できる
- GPT Image 2.0で作成した背景やスプライトをそのまま活用できる
- WebGL用3Dモデルを用意せず、ブラウザで軽く動作する

今回、高速時の画角拡大、地平線移動、カメラロール、路面反射、頭上ゲート、速度線、車体バウンドを追加しました。

## 不採用: ドット絵トップダウン方式

速度表現は作りやすいものの、現在の高精細3D風キャラクターと混在させると画風が崩れます。採用する場合は、12人×全方向、コース、アイテム、UIをすべて同一ピクセル密度のドット絵としてGPT Image 2.0で再制作する必要があります。

## 将来案: WebGLフル3D

最も自由度は高い一方、キャラクターとカートの3Dモデル、リグ、アニメーション、コースメッシュが必要です。現在の2D画像資産だけでは角度やライティングの一貫性を保てないため、現段階では採用していません。

## 次期分岐描画: Dual Corridor Projection v1

実装状況（2026-07-26）: エメラルドジャングル遺跡の分岐区間へ先行実装済み。選択ルート基準の独立した画面中心・道路幅・高さ・角度、中央密林の前景遮蔽、左右ルート固有の立体物投影を同時検証しています。

現在の `independent-route-edges-v3` は、左右の路面端と高低差を別々に描画していますが、投影元の中心線・地平線・奥行き配列は共通です。そのため、ミニマップ上では離れたルートでも、レース画面では「太い道路の隣接レーン」に見えます。`visualGap` を増やすだけでは、カーブ方向、消失点、遮蔽が共通のままなので根本解決になりません。

次期方式では、分岐中だけ左右を独立した道路回廊として扱います。

1. 左右のルートがそれぞれ独立した中心線、カーブ、坂、物理距離を持つ
2. 選択中ルートは通常密度、非選択ルートは低密度で別々に遠近投影する
3. 各ルートが固有の `cx / y / heading / maxY` を持ち、森・崖・建物などの中央地形で相互に遮蔽する
4. カメラは選択ルートの接線と消失点へ追従し、隣ルートは遠景として扱う
5. カート、アイテム、ロケット、順位、ミニマップを共通の `routeId + routeDistance` で参照する
6. 合流時だけ両ルートの正規化距離を本線距離へ戻す

Canvas 2Dのまま実装可能です。選択ルートを112セグメント、非選択ルートを48セグメント程度に抑えれば、WebGL化せずスマホ負荷も管理できます。

## 次期沿道描画: Biome Volume System v1

実装状況（2026-07-26）: エメラルドジャングル遺跡へ正式版 `zoned-cluster-lod-v1` を実装済みです。全周を8ゾーンへ分け、固定シードの遠景・中景・近景クラスター、ゾーン別樹冠、霧・木漏れ日の大気層を距離別LODで描画します。分岐中央の `themed-dual-corridor-volume-v2` と組み合わせ、選択していないルートも密林で遮蔽します。

現在の沿道は、道路距離に対して一定間隔で単体スプライトを配置するため、「看板が順番に流れていく」見え方になりやすい構造です。次期方式では、単体オブジェクトではなく、区間ごとの景観ボリュームを配置します。

- 遠景: 山、城壁、巨大樹、都市群などの連続シルエット
- 中景: 建物群、遺跡壁、樹木群、滝など3～6個をまとめたクラスター
- 近景: 草、根、岩、観客、灯具など道路端を縁取る小型群
- 天井景: 樹冠、橋脚、配管、屋根など画面上側を覆うフレーミング層
- 大気層: 霧、木漏れ日、雨幕、胞子、蒸気を距離別に合成

配置は `course + zone + seed` で固定し、画面へ入るたびに同じ景観になるようにします。描画負荷は距離別LODとクラスター単位のキャッシュで制御します。

### ジャングル専用構成

1. 巨大樹の幹を左右の連続壁として配置
2. 樹冠を画面上端に連続させ、空の面積を区間ごとに30～70%へ制御
3. 遺跡壁・根・シダを3～6個の塊で配置し、単体看板感をなくす
4. 滝と霧を中景、葉とツタを近景に分けて奥行きを作る
5. 木橋区間は橋脚と谷底を描き、道路外を平地に見せない
6. 分岐中央には密林・岩壁・遺跡を置き、反対ルートを部分的に隠す

## コースギミック共通基盤

実装状況（2026-07-26）: ジャングル左ルートの落下丸太と右ルートの石扉を、同一サイズの4×2アニメーションシート、周期状態、衝突、NPC減速判断を持つルート対応ハザードとして先行実装済みです。

新しいギミックは `z / lane / routeId / state / collision / draw / audio` を持つ共通エンティティとして実装します。これによりプレイヤーとNPCが同じ判定を使い、分岐ルートにも配置できます。

優先順位は次の通りです。

| 優先 | 共通ギミック | 活用例 |
|---|---|---|
| 1 | 時限式レーン障害物 | 歯車ゲート、落下丸太、回転ハンマー |
| 2 | 路面ハザード帯 | 水たまり、泥、薄氷、クリーム床 |
| 3 | 噴出ギミック | 蒸気、水柱、泡、火花 |
| 4 | 高低差ギミック | 跳ね橋、崩れる床、低重力ジャンプ |
| 5 | ルート連動ギミック | 開閉門、近道ゲート、片側だけの報酬列 |

コース固有ギミックの実装:

- スイーツ: クリーム減速帯
- スチーム: 予告付き蒸気噴出
- ネオン: 左右へ動く光壁
- レイン: 深い水たまり
- ロイヤル: レーンを横切るキャンディ樽
- オーロラ: ひび割れる薄氷
- ジャングル: 左ルートの落下丸太、右ルートの石扉
- さくら: 花びらの横風
- コーラル: 交互に噴き上がる水柱
- ファントム: 周期的に実体化する幽霊門
- ルナティック: 低重力ジャンプ床

全ギミックは `shared-world-hazard-v1` として `z / lane / route / phase / collision / AI avoidance / draw / audio` を共有し、プレイヤーとNPCへ同じルールを適用します。

## Biome Volume System v2 — 全11コース

実装状態（2026-07-26）: `all-course-zoned-cluster-lod-v2` を全11コースへ展開済み。

- 各コースを4つ以上の固有景観ゾーンへ分割。エメラルド遺跡のみ8ゾーン。
- `course + zone + seed` から固定クラスターを生成し、毎回同じ景観リズムを再現。
- 遠景・中景・近景・天蓋・大気の5層を共通投影へ接続。
- HIGHでは全クラスター、LIGHTでは近景を間引く距離LODを使用。
- トンネル区間はクラスターを抑止し、出口の向こう側に景観が透けないようにする。
- 各コース既存のGPT Image 2.0景観アトラスを再利用し、テーマ外オブジェクトを混在させない。

## コース固有ギミック・アニメーションアトラス

蒸気口、キャンディ樽、ウォータージェット、幽霊門は
`course-dynamic-gimmicks-gpt2-v1` へ置き換え済み。

- GPT Image 2.0生成
- 4列×4行
- 1セル256×256px
- 行: 蒸気口 / キャンディ樽 / ウォータージェット / 幽霊門
- 列: 待機 / 予告 / 作動 / 収束
- 全セルを共通基準線・共通縮尺で再パックし、隣接コマの混入を自動検査
